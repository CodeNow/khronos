machine:
  node:
    version: 0.12.7
  environment:
    NODE_ENV: test
dependencies:
  post:
    - npm install -g npm@latest
test:
  pre:
    - ulimit -n 10240
  override:
    - npm run lint
    - npm run unit
    - npm run functional
